interface AnimalStuff {
    // an interface is an abstract, but REALLY abstract
	// no normal methods allowed
	// like a "list of stuff that needs to be implemented!"
	boolean good = true; // in interfaces: public, static, final.
	
	public void shit();
	public void breathe();
}